package com.company.questions;

public class YoungestEmployees {
    //SELECT u.uin, e.name
    //FROM employee_uin AS u
    //RIGHT JOIN employee AS e
    //ON u.id = e.id
    //WHERE e.age < 25
    //ORDER BY e.name ASC;
}
